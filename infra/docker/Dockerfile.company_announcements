FROM python:3.12-slim

WORKDIR /app
RUN pip install --no-cache-dir uv

COPY pyproject.toml ./
COPY packages/shared ./packages/shared
COPY services/company_announcements ./services/company_announcements

RUN uv pip install --system -e packages/shared -e services/company_announcements

ENV PYTHONPATH=/app/packages/shared/src:/app/services/company_announcements/src
EXPOSE 8003
CMD ["uvicorn", "company_announcements.api.main:app", "--host", "0.0.0.0", "--port", "8003"]
